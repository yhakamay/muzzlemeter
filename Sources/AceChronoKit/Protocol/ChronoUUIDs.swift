import Foundation

/// AC6000 MKIII BT の BLE サービス / キャラクタリスティック UUID。
///
/// TODO: リバースエンジニアリング（`acechrono-sniff dump` および AceSoft APK の
/// jadx デコンパイル結果）でサービス UUID / notify・write キャラクタリスティック UUID が
/// 判明したらここに定数として埋める。判明した内容は `docs/PROTOCOL.md` にも記録する。
public enum ChronoUUIDs {}
