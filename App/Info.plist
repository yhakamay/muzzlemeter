<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- 残りのキーは GENERATE_INFOPLIST_FILE と INFOPLIST_KEY_* から合成される。
	     ここには「ビルド設定では配列を書けないキー」だけを置く。 -->
	<key>CFBundleLocalizations</key>
	<array>
		<string>en</string>
		<string>ja</string>
	</array>
	<!-- App Store validation (ITMS-90713) wants CFBundleIconName at the top level.
	     actool only writes it nested under CFBundleIcons, so declare it here; the
	     value is the app icon set's name in App/Muzzlemeter/Resources/Assets.xcassets. -->
	<key>CFBundleIconName</key>
	<string>AppIcon</string>
</dict>
</plist>
